# 2. Architecture Constraints

_Part of [Architecture Workflow (Yggdrasil) — Architecture (arc42)](../README.md) · [§2](README.md)._

| Constraint | Source | Effect |
| --- | --- | --- |
| C-1 Every SKILL.md carries the five sections `## Purpose`, `## When to Use`, `## Workflow`, `## Quality Criteria`, `## Anti-Patterns` in order; `name:` equals directory slug; `description:` names no agent. | `scripts/validate.sh` Checks 1–3, 6 | New workflow skill shape fixed. |
| C-2 `agents/odin-*.md` are generated; byte-for-byte drift fails validation. Edit `scripts/odin-generator/*.template.md` / `*.fragment.md`, then run `scripts/generate-odin-agents.sh`. | `validate.sh` Check 4; context research §1 | Prompt changes go through templates only (ADR-0008). |
| C-3 Subagent skills (`kvasir-*`, `heimdall-*`, `brokk-*`, `mimir-*`, `bragi-*`) may not name another agent; the owner is derived from the slug prefix and the match is case-insensitive with word boundaries — so another agent's **skill slug** (e.g. `kvasir-software-architecture` inside a `brokk-*` file) is a violation too. | `validate.sh` Check 5, lines 480–489; header comment line 23 | Extended Kvasir/Heimdall/Brokk skills say "the requesting agent"/"the drafting step"/"the reviewer", never a foreign slug. Renaming the template to `brokk-arc42-template` makes its current line 43 (`kvasir-software-architecture`) a violation that WP-0 must reword; `kvasir-software-architecture` may not name `brokk-arc42-template` — hence the dependency removal (ADR-0011). |
| C-4 Files under mandatory dirs may not reference optional-only skill slugs; "optional" is derived as *any* `skills/*/` directory not listed in `MANDATORY_SKILL_DIRS`. | `validate.sh` Check 9 (optional roots derived at check time); `setup.sh:370–378` installs non-listed dirs as optional bundles | A new `skills/architecture/` directory **must** be added to both lists or (i) mandatory-only installs lack the architecture skills and (ii) `engineering/odin-engineering-workflow`'s references to them fail Check 9. The new workflow skill must not reference e.g. `brokk-system-prompts`; that reference lives only in implementation briefs. |
| C-5 Command files: `description` required; `agent` must be an Odin variant; `subtask: false`; non-empty body. | `validate.sh` Check 8; `commands/yggdrasil/engineer.md` | Shape of `architect.md` fixed. |
| C-6 Adding a new mandatory feature directory requires editing `MANDATORY_SKILL_DIRS` in both `setup.sh:167` and `validate.sh:762`, the descriptive comments/warnings (`setup.sh:362`, `validate.sh:40,443`, `config-home/generate-capabilities.sh:155`, README line 273), and — because `setup.sh` never deletes under the destination — a cleanup of relocated skill directories in already-deployed config homes. | `setup.sh:167,355–378`; `validate.sh:762`; `grep rm -rf setup.sh` → none | Accepted consequence of ADR-0009 (user-directed `architecture/` directory); owned by WP-0. |
| C-10 The arc42 license notice hardcodes the template skill's slug and path: `kvasir-arc42-template skill:` (line 6) and `skills/engineering/kvasir-arc42-template/SKILL.md` (lines 8, 66). | `LICENSES/CC-BY-SA-4.0-arc42.txt` | All three lines change to `brokk-arc42-template` / `skills/architecture/brokk-arc42-template/SKILL.md` (ADR-0009, ADR-0011, WP-0). This is the only hardcoded skill path in the repo outside README (grep over `*.md,*.sh,*.txt,*.yaml`); `mimir-codebase-context` has **no** path reference anywhere — only its own frontmatter, README lines 131/179, and two load-by-name mentions in `odin-engineering-workflow/SKILL.md` lines 14 and 46. |
| C-11 A skill may load another skill by name with the `skill` tool; loading is layout-agnostic. The capability generator derives the owning role from the slug prefix, not the directory — so renaming `kvasir-arc42-template` → `brokk-arc42-template` moves it from the Strategist to the Implementer inventory automatically. | `config-home/generate-capabilities.sh:152–158`; `setup.sh` copies whole directories | Moving skill directories breaks no load-by-name reference; only path strings (C-10, README) and the renamed slug's own `name:` frontmatter (Check 3) need updating. |
| C-12 Every Brokk or Mimir session receives a dedicated Heimdall review; Kvasir and Bragi sessions do not. | `scripts/odin-generator/shared-body.template.md:215–216` | The scaffold step (a Brokk session) costs one standing review — `heimdall-architecture-review` `Focus: scaffold` (ADR-0011). Each context skill dispatch likewise costs one (ADR-0013). |
| C-13 The engineering workflow's context step does **not** need to run before business analysis. | User direction (standing, given before the split) | Settled positioning requirement, not an option: ADR-0015 decides only *where after* business analysis it sits. Bragi's skill already treats a context Workfile as optional input (`bragi-business-analysis/SKILL.md:41`). |
| C-14 A persisted arc42 directory now exists (`docs/architecture/`, seed 2026-09-18 + 2 deltas 2026-09-19); ADR-0001–0011 are `Accepted`. Persistence is update-delta, whole-file per named section, §9 rows appended only, superseded decision files never edited. | `docs/architecture/README.md` Change Log; `brokk-architecture-persistence` steps 5; `kvasir-software-architecture` ADR format ("you do not edit the superseded file") | Revision 4 must be authored as a delta whose replaced sections are complete *and* carry the persisted post-seed fixes (R-16); ADR-0004 stays `Accepted` in file and log, and ADR-0014's row and `Related` state the supersession. |
| C-15 `Focus: context` in `heimdall-engineering-review` (lines 69–81) checks completeness against the single 12-part output contract and BLOCKs on missing test-infrastructure facts for any code-touching objective. | live file | Two differently-shaped context Workfiles cannot share that checklist unchanged (ADR-0013). |
| C-16 **Brokk never writes a Workfile.** "Mimir, Kvasir, Heimdall, and Bragi write Workfiles … to the Yggdrasil Workspace; Brokk reads Workfiles as inputs but does not write them." Brokk's outputs are Artifacts — files in the target project. | `scripts/odin-generator/shared-body.template.md:49` (§ Yggdrasil Workspace); Artifact definition at line 45 | The delivered scaffold step (`brokk-arc42-template` writing `NN-architecture-arc42.md`) violates this; ADR-0016 repairs it. Every review of the scaffold checked content against the ratified spec, not against this rule — R-21 adds a standing-doctrine item to `heimdall-architecture-review`'s common checks. |
| C-17 Git does not track empty directories; a scaffold committed as bare folders vanishes. | git semantics | Every scaffolded section folder must hold at least one file — the generated section index (ADR-0017). |
| C-18 The persisted `docs/architecture/` is a **flat** arc42 directory (12 `NN-*.md` files + `decisions/`, `README.md`), seed + 3 deltas, ADR-0001–0015 `Accepted`. | `ls docs/architecture`; README Change Log | The folder-per-section layout (ADR-0018) is a different shape. **D-1 = A (decided 2026-09-19):** the existing document is migrated by `git mv` as the first act of R5's persistence. Both Brokk skills *recognize* the flat shape (`flat directory (legacy)`) for classification and migration only — no step writes a flat-shaped document; a flat target without migration direction is a blocking report item. |
| C-7 New skills are invisible to planning until `config-home/generate-capabilities.sh` runs (auto on `setup.sh`). | README lines 288–294, 335–343 | Required post-step (AC-16, NF-9). |
| C-8 arc42 guidance text is CC BY-SA 4.0; the notice must be present iff `arc42 guidance §N` blocks survive in a document. | template skill Workflow step 9; `LICENSES/CC-BY-SA-4.0-arc42.txt` | Rule is now split by role: the scaffold (Brokk) always places the notice because the skeleton carries guidance; the drafter (Kvasir) deletes the notice iff it deleted every guidance block (ADR-0011). Document mode inherits the same rule. |
| C-9 Settled decisions from the brief: one command; conditional own context step (skippable via reviewed context Workfile); document mode persists by default to `docs/architecture/` after mandatory review; review gate inside the new workflow; extend `kvasir-software-architecture` in place. | Task brief | Treated as constraints; ADRs record the *how*. |
