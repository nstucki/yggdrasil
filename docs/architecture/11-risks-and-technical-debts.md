# 11. Risks and Technical Debts

_Part of [Architecture Workflow (Yggdrasil) — Architecture (arc42)](README.md)._

| Risk / debt | Impact | Likelihood | Mitigation or acceptance |
| --- | --- | --- | --- |
| R-1 Trigger overlap: "architecture" language matches both Engineering and Architecture checks | Wrong workflow (heavy engineering run or no implementation) | Medium | Precedence rule in both § Workflows entries (ADR-0002); Heimdall `system-prompt-review` simulates ≥4 boundary phrasings |
| R-2 Document-mode checklist in `heimdall-architecture-review` under-specified → document mode BLOCKED spuriously or waved through | Persistence of unreviewed content or blocked runs | Medium | Explicit document-mode item list in 5.1.5; review simulation of one as-is Workfile |
| R-9 Relocated skills leave stale copies in already-deployed config homes (`setup.sh` never deletes) → two installed skills with the same `name` | Ambiguous skill loading; old text silently used | High without mitigation | `setup.sh` cleanup of the four relocated slugs (incl. the old `kvasir-arc42-template` name) under deployed `engineering/` (§5.1.10, WP-0); README upgrade note; Q-10 measures it |
| R-10 Hardcoded path strings missed on the move (`LICENSES/CC-BY-SA-4.0-arc42.txt`, README) | Stale license scope statement; misleading docs | Medium | Grep found exactly two path hits outside README (C-10); WP-0 and WP-5 own them; WP-5 integration trace re-greps `skills/engineering/kvasir-arc42-template` → 0 hits |
| R-11 `heimdall-engineering-review` `Focus: integration` now depends on loading `heimdall-architecture-review` by name at review time | If the load fails, the persisted architecture goes unchecked in the engineering path | Low | Mandatory→mandatory reference (Check 9 passes once WP-0 lands); the bullet says "its blocking conditions are blocking here" so a failed load is itself a finding; precedent for skill-loads-skill exists (C-11) |
| R-3 Document mode requires system-scoped Mimir context on brownfield repos — cost higher than NF-1 envisaged | Slower standalone runs | High | Accepted (settled decision); shape verdict records `context=no` whenever conversation already established it; user can supply context |
| R-12 The scaffold step adds a Brokk dispatch **and** a mandatory standing review to every run, including engineering delegation (Δ = +2); the review's value is low relative to its cost | Slower runs; NF-3 no longer holds | Certain | Accepted by user direction (ADR-0011); `Focus: scaffold` kept ≤ 8 items (Q-13); the step also removes Kvasir's repository look-up, partly offsetting the cost in Kvasir's session; revisit if the framework later exempts mechanical Brokk sessions from Subtask Review |
| R-13 The template's rename re-derives its Check 5 owner as `brokk`; any residual `kvasir`/`heimdall`/`odin` token in its 700+ lines fails validation | validate.sh failure on WP-0 | Medium | Grep at design time found exactly two agent-token hits (line 2 frontmatter, line 43); WP-0 fixes both and runs Check 5 before finishing; WP-3's rewrite is re-scanned |
| R-14 Skeleton drift: Brokk scaffolds a skeleton whose heading levels or header fields differ from what `heimdall-architecture-review` `Focus: document` expects | Spurious BLOCKED at step 5 | Low–Medium | Both derive from the same § Skeleton text; `Focus: scaffold` re-checks header fields and heading levels before Kvasir starts; WP-5's cross-file trace covers I-5 end to end |
| R-15 (R4) Engineering delegation cost: the architecture-context pair now always runs on brownfield repos inside the delegation; when the engineering-context step also fires, the engineering path pays 4 context dispatches where R3 paid 2 | Slower engineering runs in the both-fire case | Medium (both fire whenever a brownfield objective both restructures modules and changes pinned behavior) | Accepted by user direction (purpose-scoped skills); the engineering step's narrowed criteria and write-set scoping keep it shorter than the old shared gate; Q-5 records Δ honestly; revisit only with a framework-level review exemption |
| R-16 (R4) **The Workfile is behind the persisted document**: two post-seed deltas (Mermaid quoting in §3/§5/§6, I-3 `not-reached` + failure-contract sentence, §5.1.7/§6.1 wording) exist only in `docs/architecture/`. Whole-file replacement of §3/§5/§6 from an un-backported Workfile would regress them | Silent regression of reviewed fixes in a persisted Artifact | High if unmitigated | Backported in this revision (marked "*Backported from the persisted 2026-09-19 delta*"); Q-17 verifies by `git diff` after persistence; the R4 persistence reviewer (`Focus: persistence`) must diff each replaced section against the pinned baseline for these specific hunks |
| R-17 (R4) Renaming a shipped skill leaves a stale deployed copy under `architecture/mimir-codebase-context/` that the existing cleanup loop (engineering-only) does not remove; the old `name:` is then re-harvested into the capability inventory | Two Mimir context skills with overlapping descriptions visible to planning; wrong skill dispatched | High for existing installs | `setup.sh` gains an `architecture/` sweep for the old slug (§5.1.10); README upgrade note; Q-10-style check after install (0 hits for `mimir-codebase-context`) |
| R-18 (R4) Business analysis without behavior facts (C-13) may write acceptance criteria that contradict current behavior | Rework at the engineering-context step or at TDD | Medium | Settled by user direction; mitigation: the engineering-context step reports contradictions between ACs and current behavior in its `Not Examined`/contradictions items, and the plan checkpoint (step 6) surfaces them before implementation |
| R-19 (R4) Post-delivery change surface: five live skill files, two workflow doctrine files, `setup.sh`, README, and a persisted arc42 directory with `Accepted` records must be edited in place under never-overwrite discipline — unlike R1–R3, which edited not-yet-written files | Higher review load; a missed cross-reference (e.g. `NN-context-<area>.md` naming, `Focus: context` reviewer, `context=` in any brief) breaks a shipped path | Medium–High | Sequential scaffold package first (rename + cleanup), then parallel edits with per-file baselines pinned to `8e6c9cb`; WP-R4-4 runs the full cross-file trace and `validate.sh`; grep gates: 0 hits for `mimir-codebase-context` and `context=<Workfile path` outside the workspace |
| R-4 Step renumbering in the engineering skill breaks cross-references (README, invariant markers, Check 7) | validate.sh failure or stale docs | Medium | Renumber in one package; grep for "step 5"/"step 9" in README and templates; run validate.sh |
| R-5 As-is ADRs at `Status: Proposed` read oddly for decisions already in effect | Reader confusion | Low | `Kind: as-is` line; Brokk promotes to Accepted on persistence; revisit if a `Recorded` status is later wanted (debt) |
| R-6 Skill-load composition relies on Odin executing a nested step list faithfully | Skipped gate or double review | Low–Medium | Return block mandatory; callee's shape verdict records every skipped step with reason; engineering Quality Criteria require the return block before the TDD plan |
| R-7 Check 5/Check 9 word-boundary violations in extended specialist skills | validate.sh failure | Medium | Wording rule in C-3/C-4; run validate.sh per package |
| R-8 OpenCode command→agent routing not verified (context research gap) | `/yggdrasil/architect` fails to route | Low | Copy `engineer.md` shape byte-for-byte except body; smoke-run after install |
