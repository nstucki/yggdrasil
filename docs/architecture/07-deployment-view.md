# 7. Deployment View

_Part of [Architecture Workflow (Yggdrasil) — Architecture (arc42)](README.md)._

_Omitted — the installation mechanism is unchanged (`setup.sh` copies every listed mandatory directory and `commands/yggdrasil/`); the only deployment-relevant changes are the `MANDATORY_SKILL_DIRS` entry and the relocated-copy cleanup, both specified in §5.1.10 and ADR-0009. Deployed layout after install: `~/.config/opencode/skills/yggdrasil/architecture/` holding the six architecture skills (`odin-architecture-workflow`, `mimir-codebase-context`, `brokk-arc42-template`, `kvasir-software-architecture`, `heimdall-architecture-review`, `brokk-architecture-persistence`); `engineering/` holding `odin-engineering-workflow`, `bragi-business-analysis`, `brokk-test-driven-development`, `heimdall-engineering-review` only._
