# 5.1.7 Consumed block `brokk-architecture-persistence`

_Part of [Architecture Workflow (Yggdrasil) — Architecture (arc42)](../README.md) · [§5](README.md)._

- Moved to `skills/architecture/brokk-architecture-persistence/SKILL.md` by WP-0 (the move itself changed no content): inputs unchanged (reviewed Workfile, ratification record, baseline, location, migration flag). Document mode supplies a ratification record listing the as-is ADR IDs ratified. **Open issue closed:** the index footer and the When to Use entry were reworded to name both callers. Content has changed since delivery — those two wording fixes, plus the two-level heading-promotion rule for decision records and the not-persisted disposition of Workfile-level process metadata. `brokk-arc42-template` — see 5.1.11.
- **R5 — Modified (ADR-0018, ADR-0020):**
  - **Purpose:** fill a **pre-scaffolded** folder layout from the reviewed Workfile's Appendix D. The skill no longer creates the structure; a target that fails the identity rule, or whose top index is not at `Status: Scaffolded` (seed) / `Accepted` (update delta), is a blocking report item — "scaffold step did not run" — never an invitation to create. The seed-collision rule and the default-location resolution leave this skill (they are the scaffolder's).
  - **Inputs (brief):** reviewed Workfile path; ratification record; pinned baseline; the scaffold step's `Scaffold result` line (target path, structure, shape); migration direction (`none | migrate flat directory | migrate single file`, default none).
  - **Workflow (rewritten steps 2–9):** 2 read the Workfile header's `Scaffold:` and `Document scope`, verify they equal the result line and the tree (identity rule; status invariant). 3 resolve the record directory (`<target>/09-architecture-decisions/`, or the project's own `docs/adr/` etc. when present — unchanged precedence). 4 **seed:** for every section, write the topic documents Appendix D names (per-document format, promotion `depth − 1`), or leave an omitted section index-only with its marker; write records at `NNNN-<slug>.md` (promotion 2, `Accepted` iff ratified); regenerate every section index and the top index (`Status: Accepted`, Change Log row `seed`). 5 **update delta, folder layout:** for each `included=` section, write its mapped documents by whole-file replacement, regenerate that section's index, **never delete** a document the new map omits (list it under "superseded documents" in the index); append §9 rows; new records; regenerate top index, append one row. 6 **update delta, flat legacy directory:** the flat shape is **never written**. With cited migration direction, run step 8 then step 5; without it, stop and report `shape=flat directory (legacy) — migration direction required` as a blocking item (the Deliverable offers the migration; nothing is persisted). 7 legacy single file / non-arc42: unchanged (single-file migration now targets the folder layout). 8 **migration (direction-gated, `git mv`-based):** flat directory → folder layout: for each **filled** `NN-<section>.md`, `git mv` it to `NN-<section>/01-<section-slug>.md` (H1 unchanged — it is already the section heading promoted by one; body unchanged; backlink line rewritten to the two-link form) and generate that section's index (Documents table: the one migrated document); for each **stub** `NN-<section>.md` (an omission marker), `git mv` it to `NN-<section>/README.md` — the stub *becomes* the omitted section's index, since a stub already carries the index's H1, backlink, and marker; regeneration then adds only the one-line description, so git's rename detection sees a clean `R` (content similarity far above the threshold) and the folder has no topic document, exactly as §5.1.13 specifies for an omitted section. **Every one of the twelve section files therefore migrates as a rename; none is deleted;** `git mv decisions/NNNN-*.md 09-architecture-decisions/`; `git mv 09-architecture-decisions.md 09-architecture-decisions/README.md` and rewrite its `Link` column to `NNNN-<slug>.md`; regenerate the top index with a Change Log row `migrated from flat directory`; remove the now-empty `decisions/`. Verify `git log --follow` on at least two moved files reaches the seed commit before proceeding. Then apply step 5 for the current delta. 9 manifest: one row per path; actions gain `moved` (migration) and `index regenerated`.
  - **Layout spec:** § The Persisted Layout is replaced by the text of 5.1.13 verbatim (the normative copy lives here; 5.1.11 carries the mirror).
  - **Location:** `skills/architecture/brokk-architecture-persistence/SKILL.md` (Purpose, § The Persisted Layout, § Per-File Format → § Per-Document Format, § Index Format (+ section index), Workflow 1–9, Quality Criteria, Anti-Patterns — "Splitting subsections into their own files" anti-pattern replaced by "Splitting below the map / splitting by habit").
  - **Fulfilled AC:** AC-5, AC-18 (persisted Artifact shape).
