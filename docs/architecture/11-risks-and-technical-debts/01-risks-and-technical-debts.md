# 11. Risks and Technical Debts

_Part of [Yggdrasil — Architecture (arc42)](../README.md) · [§11](README.md)._

R1–R9 stand from the seed document (R7 is extended by R16). R10 onward are introduced by this revision; each carries impact, likelihood, and a mitigation or an explicit acceptance.

| # | Item | Kind | Evidence | Impact | Likelihood | Mitigation / status |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | **Generator documentation drift.** `scripts/README.md:158` describes subagent assembly as three pieces; the generator concatenates five | Debt | `scripts/README.md:158` vs `scripts/generate-subagents.sh:103-121` | A maintainer edits the wrong file | Medium | None found; WP-1 touches `scripts/README.md` and may correct line 158 opportunistically |
| R2 | **Environmental constraint asserted, not verified** (`write` tool hangs) | Risk | `scripts/subagent-generator/tooling.fragment.md:6` | Dead doctrine or silent Workfile loss | Low–Medium | Regenerated into every Workfile author, Eitri included |
| R3 | **Consultation output is unreviewed until the gate** | Risk (by design) | `agents/odin-autonomous.md:152, 253` | Rework downstream | Medium | Accepted; Final Review Gate is the backstop |
| R4 | **Boundary enforcement split between mechanism and prose** | Risk | `agents/odin-autonomous.md:6-19, 38-40`; `agents/brokk.md:84-95` | Prose boundaries depend on compliance | Medium | Independent review; validator Checks 5–7 |
| R5 | **Shell permissions are glob patterns** | Risk | `agents/mimir.md:8-54`; `agents/brokk.md:8-52` | Unguarded metacharacter could exceed the surface | Low–Medium | Explicit guards; Eitri has no `bash` at all |
| R6 | **Memory-write failure path is implicit** | Debt | `skills/memories/odin-memory-system/SKILL.md` | Ambiguity on fix loops for blocked Memory writes | Low | General classification applies |
| R7 | **Install-side artifacts are invisible to the repository validator** | Risk | `scripts/README.md:72, 162-166` | Malformed grant or stale inventory detected only at runtime | Low | Inventory regenerated on every install |
| R8 | **Seed document: three skills and two Odin modes not read in full** | Debt (of the document) | seed context Workfile | Secondary evidence in §5.1.3, §8.5, §8.7 | — | Listed as gaps |
| R9 | **`scripts/ci-smoke-generator.sh` undocumented in `scripts/README.md`** | Debt | `scripts/README.md:83-103` | Discoverability | Low | WP-1 may add it while editing that README |
| R10 | **OpenCode `model:` support is unverified in this repository.** No agent carries the field; OpenCode's agent schema was not examined. If the field is ignored or misnamed, Eitri silently runs on the session default and AC-7/AC-8 fail at runtime while every repository check passes | Risk | context Workfile § Asked but Unconfirmed, Question 3; ADR-0009 open question | Configurability goal unmet; misleading manifests | Medium | Ratifier confirms against OpenCode's agent configuration documentation before WP-1; QS-12/QS-14 manual acceptance; fallback documented in README: if unsupported, the override is `opencode.json` only |
| R11 | **Out of the box, Eitri produces text-encoded imagery (SVG) only.** Raster formats need a post-install tool grant that this revision declares out of scope (C15, C16) | Risk (accepted) | ADR-0010; `01-requirements.md` § Out of Scope item 1 | A user asking for a PNG receives `Image: blocked` until a tool is granted | High until a tool is granted | Accepted and disclosed: Eitri's Boundaries, the README Eitri row, and the blocked-manifest contract all state it |
| R12 | **The specialist roster is duplicated in seven script/template locations** (ADR-0011 list) with no single source; this revision extends each in place | Debt | `scripts/generate-subagents.sh:68, 74`; `scripts/validate.sh:351, 450-451, 486, 520`; `config-home/generate-capabilities.sh:180-189, 256-269`; `scripts/ci-smoke-subagent-generator.sh:40, 68-72`; `scripts/ci-smoke-generator.sh:105-109, 121`; `scripts/odin-generator/preamble.template.md:12-18` | A future seventh specialist is missed in one list; Check 4/5 catch some omissions but not the capability generator's | Medium | `scripts/README.md` gains the enumerated touch list (QS-17); a roster manifest is deferred (ADR-0011 option B) |
| R13 | **Heimdall's image review has no visual-fitness criteria.** Review rests on `read`-ing the file and checking the manifest's compliance rows against the brief | Risk | `01-requirements.md` § Deferred item 2; §8.1 | Weak review of aesthetic fitness; structural defects still caught | Medium | Accepted for this revision; a `heimdall-image-review` criterion set is the natural follow-up |
| R14 | **`temperature: 0.7` lies outside the observed 0.1–0.5 band** and its effect on an image-capable model's text channel is untested | Risk | `agents/*.md:5`; `01-requirements.md` § Open Questions item 2 | Erratic manifests or prompts | Low | Value is a one-line template edit; Appendix A item 3 invites the ratifier to choose 0.5 for parity |
| R15 | **`read`, `glob`, and `grep` are unscoped**, exceeding the literal baseline "read the Yggdrasil Workspace" (C13) | Risk | ADR-0010 | Eitri can read any project file, like every other specialist | Low | Stated need recorded; ratifier may narrow (ADR-0010 option B) at the cost of reference-asset access; no write is widened |
| R16 | **The `opencode.json` override site is outside the framework's install footprint and its validators** (extends R7) | Risk | ADR-0009; `README.md:341-361` | A typo in the override silently falls back to the shipped default | Low–Medium | README documents the exact key path; QS-12 manual acceptance; the manifest's `Model` row makes the effective model visible per run |
| R17 | **Explicit Memory `deny` globs after image `allow` globs rely on OpenCode's specific-over-general pattern precedence** — **moot as of 2026-09-21**: the deny globs were removed by user direction (R21; ADR-0010 amendment), so no deny-after-allow ordering remains in Eitri's block | Risk (retired) | `agents/brokk.md:53-56`; `eitri.template.md:8-17`; ADR-0010 amendment | — | — | Retired by the amendment; Memory protection is by construction plus prose, as for every markdown-writing peer |
| R18 | **Requirements assumption "generator needs no logic change" is false**; the generator, validator, smoke tests, and capability generator all carry literal rosters | Debt (of the requirements) | `01-requirements.md` § Assumptions row 4 vs `scripts/generate-subagents.sh:68` | Scope is larger than the analysis assumed | Certain | Corrected here (ADR-0011, Appendix B); reported to the requesting agent |
| R19 | **Plan amendment discovered mid-execution (WP-5), added after the ratification checkpoint.** WP-2's Artifact-definition wording change at `shared-body.template.md:46` ("implementation work" → "implementation or design work") left the same sentence unmirrored in `scripts/subagent-generator/brokk.template.md:75` and `heimdall.template.md:83`; the initial package breakdown did not name those files, so the doctrine would have diverged between Odin and the two Artifact-reading specialists, and the persisted §12 glossary citation to `agents/brokk.md:75` would cite stale wording | Risk (process) — realized and mitigated | `scripts/subagent-generator/brokk.template.md:75`; `scripts/subagent-generator/heimdall.template.md:83` (both spot-checked: old wording present); `shared-body.template.md:46` (new wording present); Appendix B WP-5 | The ratified plan did not cover the full write set; WP-5 is executing under a plan the user has not yet seen amended | Certain (occurred) | WP-5 added to Appendix B with a disjoint write set, no owned AC, and byte-identity gates (Check 4, subagent smoke test); sequenced before persistence. **Requires its own ratification nod** — the requesting agent surfaces this amendment to the user; until then WP-5 is `Proposed` like every other item in this Workfile. Lesson for the roster touch list (R12): a doctrinal sentence shared across templates is also a "roster" — `scripts/README.md` should name the `## Artifact Definition` sentence as a cross-template invariant |
| R20 | **Second plan amendment discovered mid-execution (WP-6), added after the ratification checkpoint — same class as R19.** WP-2 extended the dedicated-review rule to Eitri (`shared-body.template.md:150, 225`; §8.1; ADR-0011 Consequences), but the README's user-facing statement of that rule — `README.md:18`, "Every Brokk (implementer) output is reviewed by Heimdall before it is considered final." — was not in WP-4's enumerated write set, so WP-4 correctly left it untouched and its review flagged the staleness rather than patching silently | Risk (process) — realized and mitigated | `README.md:18` (spot-checked post-WP-4: old wording present, line unshifted); `shared-body.template.md:150, 225`; Appendix B WP-6 | The README states a narrower review guarantee than the doctrine now provides; a reader would infer Eitri's output is unreviewed | Certain (occurred) | WP-6 added to Appendix B with a one-line write set, no owned AC, and validator + markdownlint gates; sequenced after WP-4 (same file) and before integration/persistence. **Requires its own ratification nod** — the requesting agent surfaces this amendment to the user; until then WP-6 is `Proposed`. Pattern lesson, with R19: two of two mid-execution gaps were *doctrinal sentences duplicated outside the roster lists* (Artifact definition in specialist templates; review guarantee in the README). The `scripts/README.md` touch list WP-1 writes should carry a third category beside "roster lists" and "counts": **duplicated doctrine sentences** — Artifact definition, review guarantee, Workfile-writer list — with every file that repeats each |
| R21 | **User-directed post-implementation deviation from the ratified design (2026-09-21).** After WP-1–WP-6 landed, the user hand-edited `agents/eitri.md` and `scripts/subagent-generator/eitri.template.md` (commits `e166fe5` "remove comment in eitri", `793a5ca` "remove memory denial for eitri", `755d6bb` "make eitri regeneratable" on `feature/designer`) and, when the divergence was surfaced, confirmed **"Keep all of it as is."** Two ratified decisions are amended: **(a) ADR-0009** — no `model:` key and no warning comment ship in the repository; model selection is `opencode.json` `agent.eitri.model` only, with no in-repo signal that it must be set; **(b) ADR-0010** — no explicit `.yggdrasil-memory/**` deny globs ship; Memory is protected by construction (narrow allows) and prose, exactly as for the four markdown-writing peers, none of which carries such a deny either | Deviation — accepted by explicit user decision | `eitri.template.md:1-25` (live: no `model:`, no Memory deny); `git show 793a5ca --stat` (2 deletions), `git show e166fe5 --stat` (13 deletions); repo-wide search: zero `yggdrasil-memory/**` deny globs in any agent; ADR-0009/ADR-0010 amendment paragraphs | (a) Silent misconfiguration is the default state on a fresh install — Eitri runs on the session default model until an operator reads the README subsection and sets the override; AC-7's "specified in the agent definition" half is unmet, only the override half holds. (b) No defense-in-depth against a future `edit`-glob widening; Eitri can place a stray image file (not a Memory entry) under `.yggdrasil-memory/` — a tidiness exposure, not a governance breach | Certain (occurred; final by user decision) | **Accepted, user-ratified.** Both points are recorded as amendments inside the affected ADRs rather than as new records, because the *choice of mechanism* stands — only its in-repository half was withdrawn. Residual mitigations: the README "Configure the Designer's image model" subsection (WP-4) is the sole discoverable instruction for (a); the manifest `Model` row exposes the effective model per run; the prose Memory boundary plus Heimdall review cover (b). Documentation consequence: §5.1.2's frontmatter block, §3.2, §8.10, and QS-18 in this Workfile are revised to the live state so the persisted architecture does not describe frontmatter that does not exist. No further ratification is sought — the user's confirmation is the ratification |
