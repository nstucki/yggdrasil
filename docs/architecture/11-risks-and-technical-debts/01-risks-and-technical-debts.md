# 11. Risks and Technical Debts

_Part of [Yggdrasil — Architecture (arc42)](../README.md) · [§11](README.md)._

Risks and debts as they stand, each with evidence, impact, likelihood, and the mitigation in place or an explicit note that none is. R1–R9 concern the framework as a whole; R10–R20 concern the architecture-documentation subsystem. A closed item keeps its number and states what closed it.

| # | Item | Kind | Evidence | Impact | Likelihood | Existing mitigation / status |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | **Generator documentation drift.** `scripts/README.md:158` describes subagent assembly as three pieces (template + workspace fragment + memory fragment); the generator actually concatenates five (template head → workspace → optional tooling → memory → workflow tail) | Debt | `scripts/README.md:158` vs `scripts/generate-subagents.sh:103-121`; directory listing shows `*.workflow.template.md`, `tooling.fragment.md` | A maintainer following the README edits the wrong file or misses the tooling/workflow pieces | Medium | None found; validator Check 4 would catch a wrongly regenerated agent but not the stale README |
| R2 | **Environmental constraint asserted, not verified.** The prohibition on the `write` tool rests on a prose claim in agent prompts that the tool hangs | Risk | `agents/bragi.md:62` | If the host behavior changes, the constraint becomes dead doctrine; if it does not, any agent that ignores it loses its Workfile silently | Low–Medium | The constraint is regenerated into every Workfile-authoring agent via `tooling.fragment.md` (`scripts/generate-subagents.sh:70, 116`) |
| R3 | **Consultation output is unreviewed until the gate.** Kvasir and Bragi sessions receive no dedicated review; a defective plan or framing is caught only at the Final Review Gate, after downstream Subtasks have consumed it. The Architecture workflow's drafting step is such a node by explicit design (ADR-0016): its gates ratify decisions and check the persisted form mechanically, and neither reads §5 or §8 for correctness | Risk (by design) | `agents/odin-autonomous.md:152, 253`; `skills/architecture/odin-architecture-workflow/SKILL.md:14, 70, 79` | Rework of downstream Subtasks when the plan was wrong; an unverified §5 interface or an invented building block can reach the persisted document | Medium | Accepted by doctrine; Final Review Gate named as backstop; the ratification checkpoint surfaces every decision's rationale; the persistence review re-derives the target facts and clears Mermaid blocks (`heimdall-architecture-review/SKILL.md:64, 88, 93`) |
| R4 | **Boundary enforcement is split between mechanism and prose.** Some boundaries are enforced by the host (permission blocks, e.g. Odin has no file tools); others only by instruction text (Brokk's scope discipline, Odin's "never paraphrase") | Risk | `agents/odin-autonomous.md:6-19, 38-40`; `agents/brokk.md:84-95` | Prose-only boundaries depend on model compliance | Medium | Independent review (§8.1) is the compensating control; validator Checks 5–7 enforce the textual invariants that can be checked statically |
| R5 | **Shell permissions are glob patterns.** `bash` allow/deny lists are command-prefix globs with explicit guards for chained and redirected `git`; coverage of other escape vectors is not evidenced | Risk | `agents/mimir.md:8-54`; `agents/brokk.md:8-52` | A permitted prefix with an unguarded metacharacter could exceed the intended surface | Low–Medium | Explicit `git*&&*`, `git*;*`, `git*|*`, `git*$()*`, backtick, and redirection denies; `"*": deny` default for Mimir |
| R6 | **Memory-write failure path is implicit.** "BLOCKED → stop" for Memory writes is inferred from the skill text, not stated | Debt | `skills/memories/odin-memory-system/SKILL.md` (no explicit failure branch) | Ambiguity about whether a fix loop applies to a blocked Memory write | Low | General Failed Review Classification (`agents/odin-autonomous.md:267-279`) applies by default |
| R7 | **Install-side artifacts are invisible to the repository validator.** The generated `capability-inventory` skill and the operator's `custom-capabilities.yaml` live only in the configuration home; validator Check 6 asserts only that the repo scaffold is empty. Until `setup.sh` is re-run, an installed inventory may list a skill the repository has since deleted | Risk | `scripts/README.md:72, 162-166`; `README.md:118, 122` | A malformed custom grant or a stale inventory is detected only at runtime by Odin, which may route to a capability that no longer exists | Low | Inventory regenerated on every install (I-6) |
| R8 | **Parts of the as-is record rest on secondary evidence.** `odin-deliberation-council`, the optional skill bundles, `odin-guided.md`, and `odin-interactive.md` are described from `README.md` and the generator structure rather than from full reads | Debt (of the record) | `README.md:352-366, 416-433`; `scripts/README.md:11-16` | §5.1.3 and §8.7 may understate or misstate those blocks' internals | Low | A delta that re-authors those documents from direct reads closes it (I-5 makes that a per-document act) |
| R9 | **Auxiliary script undocumented.** `scripts/ci-smoke-generator.sh` exists but is not described in `scripts/README.md` | Debt | directory listing of `scripts/`; `scripts/README.md:83-103` names only the two role-specific smoke tests | Minor discoverability gap | Low | None found |
| R10 | **README does not distinguish the maintainer from the operator.** `README.md:265` warns "After installation, do not edit files in this repository … Changes made to the repo after install will be lost on the next upgrade", while `setup.sh:349-393` copies the repository *over* the installed tree — so for a framework maintainer the loss runs the other way: an edit made only in the installed copy is overwritten at the next `setup.sh`. The warning is correct for the install-site operator (§1.3) and misleading for the maintainer | Debt (documentation) | `README.md:265-267`; `setup.sh:349-393`; §1.3 rows Framework maintainer / Install-site operator | A maintainer who follows the warning edits the installed copy, tests it, and loses the change at the next upgrade; the repository never receives it | Medium | ADR-0009 fixes the repository as the source for framework changes and adds the byte-identity check (QS-15); the README wording is non-architecture documentation and is left to a documentation change |
| R11 | **Relevance is a judgment with no reviewer and no mechanical ceiling.** I-1 is applied by the author alone; the only gate that reads the persisted sections is the persistence review, and it blocks on three patterns, not on judgment (ADR-0016) | Risk (by design) | §5.2 I-4; §1.1 DOC-5; `skills/architecture/heimdall-architecture-review/SKILL.md:94` | Task-scoped prose without a tell-tale pattern ("this delta", a work-package name, rollout sequencing) reaches the project and stays until a later delta re-authors that document | Medium | The tell-tale list is concrete and the frequent cases (workspace paths, task-directory names) are mechanical (QS-9, QS-14); the ratification checkpoint surfaces the section scope and decisions; if the residue proves material, a judgment-level gate or a ceiling can be added as a later decision |
| R12 | **Carried-forward documents still contain task-scoped references.** §5.1 (the whitebox table), §5.1.1, §5.1.2, §5.1.8 cite "context Workfile § …" in their Open-issues and evidence cells; §2 C10 describes the target as "scaffolded index-only at the time of writing" — references and provenance that resolve only in a workspace or a moment that no longer exists | Debt | this document §5.1, §5.1.1, §5.1.2, §5.1.8 ("context Workfile" occurrences); §2 row C10 | A reader cannot follow those citations; the documents fail I-1 and I-3 as written | Certain (present now) | Carried forward by design of document-granular deltas (I-5); each is refreshed by the first delta that re-authors it |
| R13 | **Section-granular deltas.** Closed — ADR-0014 is in force: the persistence step replaces exactly the documents the map names and carries the rest forward (`skills/architecture/brokk-architecture-persistence/SKILL.md:134`) | Risk (closed) | §5.2 I-5 | — | — | Closed by ADR-0014 |
| R14 | **Two skills carry the layout specification by copy.** Closed — the template skill that held the second copy no longer exists; the fixed folder table lives in the persistence skill alone (`skills/architecture/brokk-architecture-persistence/SKILL.md:42`). A narrower duplicate remains as R17 | Risk (closed) | directory listing of `skills/architecture/` (five skills) | — | — | Closed by ADR-0017; residue tracked as R17 |
| R15 | **Subsystem quality-goal blocks may accumulate in §1.2.** §1.2 carries a second goal table for the architecture-documentation subsystem; deltas for other subsystems could add more | Risk | this document §1.2 | §1.2 loses its "three to five ranked goals" shape and becomes a catalogue | Low–Medium | Rule stated in §1.2: a subsystem block is added only when a delta's decisions are driven by goals absent from the system ranking, and it names the decisions it drives; a later refresh may fold matured subsystem goals into the system ranking |
| R16 | **The rules are prose enforced by prose.** I-1 through I-7 are skill text applied by model sessions; nothing in `validate.sh` checks them | Risk | §5.2; `scripts/README.md:63-81` (eight checks, none content-level) | Compliance depends on model adherence, like R4 | Medium | The persistence review's mechanical items (greps, identity rule, record numbering, map ⇔ diff) are the compensating control for the pattern-matchable subset (QS-9, QS-14, QS-16); QS-15 is scriptable |
| R17 | **The section-description column exists in two skills.** The twelve one-line section descriptions are the `Holds` column of the drafting skill's § Document Shape and the last column of the persistence skill's fixed folder table, required word-identical; no validator compares them | Debt | `skills/architecture/brokk-architecture-persistence/SKILL.md:42` ("word-identical … a two-file edit"); `skills/architecture/kvasir-software-architecture/SKILL.md` § Document Shape | A one-file edit leaves generated section indices describing a section differently from the drafting rule | Low–Medium | Both skills name the coupling in their text; a `diff` of the two columns is the manual check |
| R18 | **Historical decision records carry superseded statements.** ADR-0002, ADR-0010 – ADR-0014 name two modes, a `Focus: document` review checklist, a template skill, and an Appendix D that no longer exist; the persistence contract never edits an existing record (`skills/architecture/brokk-architecture-persistence/SKILL.md:134`), so the corrections live only in ADR-0015 – ADR-0018 and the topic documents | Debt (of the record) | `docs/architecture/09-architecture-decisions/0010-*.md` – `0014-*.md`; §9 | A reader who opens an old record without the log takes a superseded clause for the current rule | Medium | Each superseding record names what it supersedes under Related; §4 item 12 and §5.2 state the current form; the log is the entry point |
| R19 | **A skipped context gate can leave §5 ungrounded.** Step 1 is conditional; the skip is recorded with its reason, but an enclosing plan that mistakes an engineering-context Workfile for structural evidence produces a draft with no reviewed structural input and no review of its own | Risk | `skills/architecture/odin-architecture-workflow/SKILL.md:37, 91` | Building blocks and interfaces in §5 rest on the drafter's own spot-checks only | Low–Medium | The skill names the engineering-context Workfile as never a skip reason and requires the skip reason to be recorded; the ratification checkpoint surfaces `context skipped — <reason>` (`:45`) |
| R20 | **The persistence grep for `.yggdrasil-workspace` is a false positive on a document that describes the workspace.** The persistence review greps the whole persisted target for `.yggdrasil-workspace` and blocks on any hit; this document legitimately names that path as a building block (§5.1.5), a store (§4 item 5), a glossary term (§12), and a tell-tale pattern (§5.2 I-1) — in carried-forward and re-authored documents alike | Risk (of self-documentation) | `skills/architecture/heimdall-architecture-review/SKILL.md:94, 97`; this document §4, §5.1.5, §5.2 I-1, §12 | Every persistence of this repository's own architecture document trips a blocking condition whose intent — catching a run-local reference — the hits do not match; the reviewer must distinguish a doctrine mention from a run-local path by reading, which the item's wording does not allow | High for this target; none for a target that does not describe the framework | None mechanical. The grep pattern that matches the run-local form without matching doctrine is `.yggdrasil-workspace/[0-9]{8}-`; narrowing item (g) to it, or exempting a path the document defines in §12, is a later decision |
