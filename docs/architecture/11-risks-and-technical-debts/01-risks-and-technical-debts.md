# 11. Risks and Technical Debts

_Part of [Yggdrasil — Architecture (arc42)](../README.md) · [§11](README.md)._

Risks and debts as they stand, each with evidence, impact, likelihood, and the mitigation in place or an explicit note that none is. R1–R9 are carried forward from the as-is record (R8 restated without run provenance under the refresh rule of §5.2 I-1); R10–R16 are introduced or surfaced by this delta.

| # | Item | Kind | Evidence | Impact | Likelihood | Existing mitigation / status |
| --- | --- | --- | --- | --- | --- | --- |
| R1 | **Generator documentation drift.** `scripts/README.md:158` describes subagent assembly as three pieces (template + workspace fragment + memory fragment); the generator actually concatenates five (template head → workspace → optional tooling → memory → workflow tail) | Debt | `scripts/README.md:158` vs `scripts/generate-subagents.sh:103-121`; directory listing shows `*.workflow.template.md`, `tooling.fragment.md` | A maintainer following the README edits the wrong file or misses the tooling/workflow pieces | Medium | None found; validator Check 4 would catch a wrongly regenerated agent but not the stale README |
| R2 | **Environmental constraint asserted, not verified.** The prohibition on the `write` tool rests on a prose claim in agent prompts that the tool hangs | Risk | `agents/bragi.md:62` | If the host behavior changes, the constraint becomes dead doctrine; if it does not, any agent that ignores it loses its Workfile silently | Low–Medium | The constraint is regenerated into every Workfile-authoring agent via `tooling.fragment.md` (`scripts/generate-subagents.sh:70, 116`) |
| R3 | **Consultation output is unreviewed until the gate.** Kvasir and Bragi sessions receive no dedicated review; a defective plan or framing is caught only at the Final Review Gate, after downstream Subtasks have consumed it | Risk (by design) | `agents/odin-autonomous.md:152, 253` | Rework of downstream Subtasks when the plan was wrong | Medium | Accepted by doctrine; Final Review Gate named as backstop; the Architecture workflow's design review (step 5) is the one exception |
| R4 | **Boundary enforcement is split between mechanism and prose.** Some boundaries are enforced by the host (permission blocks, e.g. Odin has no file tools); others only by instruction text (Brokk's scope discipline, Odin's "never paraphrase") | Risk | `agents/odin-autonomous.md:6-19, 38-40`; `agents/brokk.md:84-95` | Prose-only boundaries depend on model compliance | Medium | Independent review (§8.1) is the compensating control; validator Checks 5–7 enforce the textual invariants that can be checked statically |
| R5 | **Shell permissions are glob patterns.** `bash` allow/deny lists are command-prefix globs with explicit guards for chained and redirected `git`; coverage of other escape vectors is not evidenced | Risk | `agents/mimir.md:8-54`; `agents/brokk.md:8-52` | A permitted prefix with an unguarded metacharacter could exceed the intended surface | Low–Medium | Explicit `git*&&*`, `git*;*`, `git*|*`, `git*$()*`, backtick, and redirection denies; `"*": deny` default for Mimir |
| R6 | **Memory-write failure path is implicit.** "BLOCKED → stop" for Memory writes is inferred from the skill text, not stated | Debt | `skills/memories/odin-memory-system/SKILL.md` (no explicit failure branch) | Ambiguity about whether a fix loop applies to a blocked Memory write | Low | General Failed Review Classification (`agents/odin-autonomous.md:267-279`) applies by default |
| R7 | **Install-side artifacts are invisible to the repository validator.** The generated `capability-inventory` skill and the operator's `custom-capabilities.yaml` live only in the configuration home; validator Check 6 asserts only that the repo scaffold is empty | Risk | `scripts/README.md:72, 162-166`; `README.md:118, 122` | A malformed custom grant or a stale inventory is detected only at runtime by Odin | Low | Inventory regenerated on every install |
| R8 | **Parts of the as-is record rest on secondary evidence.** `odin-engineering-workflow`, `odin-deliberation-council`, the optional skill bundles, `odin-guided.md`, and `odin-interactive.md` are described from `README.md` and the generator structure rather than from full reads | Debt (of the record) | `README.md:352-366, 416-433`; `scripts/README.md:11-16` | §5.1.3, §8.5, and §8.7 may understate or misstate those blocks' internals | Low | A `document-existing` refresh over those blocks closes it |
| R9 | **Auxiliary script undocumented.** `scripts/ci-smoke-generator.sh` exists but is not described in `scripts/README.md` | Debt | directory listing of `scripts/`; `scripts/README.md:83-103` names only the two role-specific smoke tests | Minor discoverability gap | Low | None found |
| R10 | **README does not distinguish the maintainer from the operator.** `README.md:265` warns "After installation, do not edit files in this repository … Changes made to the repo after install will be lost on the next upgrade", while `setup.sh:349-393` copies the repository *over* the installed tree — so for a framework maintainer the loss runs the other way: an edit made only in the installed copy is overwritten at the next `setup.sh`. The warning is correct for the install-site operator (§1.3) and misleading for the maintainer | Debt (documentation) | `README.md:265-267`; `setup.sh:349-393`; §1.3 rows Framework maintainer / Install-site operator | A maintainer who follows the warning edits the installed copy, tests it, and loses the change at the next upgrade; the repository never receives it | Medium | ADR-0009 fixes the repository as the source for framework changes and adds the byte-identity check (QS-15); the README wording itself is outside this delta (non-architecture documentation) and is left for a documentation change |
| R11 | **Relevance is a judgment with no mechanical ceiling.** ADR-0013 deliberately enforces I-1 by reviewer judgment against a tell-tale list, not by a size or document-count limit | Risk (by design) | §5.2 I-4; §1.1 DOC-5 | Inconsistent verdicts across reviewers or sessions; slow drift toward either over-blocking or ceremony | Medium | The tell-tale list is identical in the drafting and the review skill and names concrete patterns (QS-9, QS-14 are mechanical for the pattern-matchable subset); if verdicts prove inconsistent, a ceiling can be added as a later decision |
| R12 | **Carried-forward documents still contain task-scoped references.** §5.1.1, §5.1.2, §5.1.3, §5.1.8 (and 5.1's table) cite "context Workfile § …" in their Open-issues and evidence cells — references that resolve only in a workspace that no longer exists | Debt | this document §5.1–§5.1.8 ("context Workfile" occurrences) | A reader cannot follow those citations; the documents fail I-3 as written | Certain (present now) | Carried forward verbatim by design of this delta (not re-authored, so the refresh rule does not apply); to be refreshed by the first delta that re-authors each document, or by a `document-existing` refresh once ADR-0014 makes that a per-document act |
| R13 | **Section-granular deltas until ADR-0014 lands.** Under the persistence rules in force, an included section's unmapped documents are listed as superseded, so any delta touching one document of §5 or §8 must carry the whole section forward (this delta carries nine §5 documents) | Risk | `skills/architecture/brokk-architecture-persistence/SKILL.md:253`; this document's Appendix D | High drafting cost, transcription risk, and an incentive to leave §5/§8 untouched — the opposite of durability | High until WP-3 completes | ADR-0014 / WP-3; if ADR-0014 is withheld, this risk is accepted and stays open |
| R14 | **Two skills carry one specification by copy.** § The Persisted Layout exists byte-for-byte in `brokk-arc42-template` and `brokk-architecture-persistence`; ADR-0014 changes it, so both copies must move together | Risk | `skills/architecture/brokk-architecture-persistence/SKILL.md:79` ("This specification is duplicated verbatim … A layout change is a two-file copy") | A one-file edit leaves the scaffold and the persistence step disagreeing about what "superseded" means | Medium | WP-3's done criterion requires the two copies to be byte-identical (a `diff` of the extracted sections); no validator check exists for it |
| R15 | **Subsystem quality-goal blocks may accumulate in §1.2.** This delta adds a second goal table for the architecture-documentation subsystem; later deltas for other subsystems could add more | Risk | this document §1.2 | §1.2 loses its "three to five ranked goals" shape and becomes a catalogue | Low–Medium | Rule stated in §1.2: a subsystem block is added only when a delta's decisions are driven by goals absent from the system ranking, and it names the decisions it drives; a later refresh may fold matured subsystem goals into the system ranking |
| R16 | **The new rules are prose enforced by prose.** I-1 through I-5 are skill text applied by model sessions; nothing in `validate.sh` checks them | Risk | §5.2; `scripts/README.md:63-81` (eight checks, none content-level) | Compliance depends on model adherence, like R4 | Medium | The design review (I-4) is the compensating control; QS-9 and QS-15 give the pattern-matchable subset a mechanical check the implementation phase can script |
